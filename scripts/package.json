{
  "name": "trendybets-scripts",
  "version": "1.0.0",
  "description": "TrendyBets data synchronization scripts",
  "scripts": {
    "start": "cd direct-sync && npm start",
    "sync": "cd direct-sync && npm run sync",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/trendybets/trendybets.git"
  },
  "author": "TrendyBets Team",
  "license": "UNLICENSED",
  "private": true
}
